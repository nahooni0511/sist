# Local Agent Rules

## Final Verification
- Before finishing any Android app change, always launch the app on an emulator, directly inspect the resulting UI screen, and only then send the final response.
- Save emulator screenshots under each Android app by creating `apps/*/.emulator_screenshot/` and storing the screenshots there.
