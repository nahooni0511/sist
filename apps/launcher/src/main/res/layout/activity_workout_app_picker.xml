<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_workout_picker_screen"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:focusableInTouchMode="true">

    <View
        android:layout_width="match_parent"
        android:layout_height="380dp"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_workout_picker_bottom_panel" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="24dp"
        android:paddingEnd="24dp"
        android:paddingBottom="20dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageButton
                android:id="@+id/backButton"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_gravity="start|center_vertical"
                android:background="@drawable/bg_workout_picker_nav_button"
                android:contentDescription="@string/workout_picker_back"
                android:padding="14dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_workout_picker_chevron_left"
                android:tint="#FFFFFFFF" />

            <TextView
                android:id="@+id/pickerTitleText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:gravity="center"
                android:paddingTop="16dp"
                android:text="@string/workout_picker_title"
                android:textAllCaps="true"
                android:textColor="#CCFFFFFF"
                android:textSize="16sp" />
        </FrameLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            android:layout_marginTop="10dp"
            android:layout_weight="1">

            <com.sistrun.launcher.CenterAuraView
                android:id="@+id/centerGlowView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:alpha="1" />

            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/cardPager"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingStart="360dp"
                android:paddingEnd="360dp" />

            <ImageButton
                android:id="@+id/previousButton"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:layout_gravity="center_vertical|start"
                android:layout_marginStart="18dp"
                android:background="@drawable/bg_workout_picker_nav_button"
                android:contentDescription="@string/workout_picker_previous"
                android:padding="18dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_workout_picker_chevron_left"
                android:tint="#FFFFFFFF" />

            <ImageButton
                android:id="@+id/nextButton"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:layout_gravity="center_vertical|end"
                android:layout_marginEnd="18dp"
                android:background="@drawable/bg_workout_picker_nav_button"
                android:contentDescription="@string/workout_picker_next"
                android:padding="18dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_workout_picker_chevron_right"
                android:tint="#FFFFFFFF" />
        </FrameLayout>

        <TextView
            android:id="@+id/appNameText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-black"
            android:gravity="center"
            android:letterSpacing="0.01"
            android:maxLines="1"
            android:textColor="#F6FFFFFF"
            android:textSize="56sp" />

        <TextView
            android:id="@+id/appDescriptionText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:lineSpacingExtra="2dp"
            android:maxLines="2"
            android:textColor="#C7FFFFFF"
            android:textSize="24sp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:text="@string/workout_picker_selection_hint"
            android:textColor="#8CFFFFFF"
            android:textSize="14sp" />
    </LinearLayout>

</FrameLayout>
